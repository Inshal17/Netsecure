system {
    services {
        ssh {
            protocol-version v2;
            rate-limit 4;
        }
        telnet;
    }
    login {
        retry-options {
            tries-before-disconnect 3;
        }
    }
}
