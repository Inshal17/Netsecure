system {
    services {
        ssh {
            protocol-version v1;
            rate-limit 20;
        }
        telnet;
        reverse-telnet;
        ftp;
    }
    login {
        retry-options {
            tries-before-disconnect 10;
        }
    }
}
