system {
    services {
        ssh {
            protocol-version v2;
            rate-limit 4;
        }
    }
    login {
        retry-options {
            tries-before-disconnect 3;
        }
    }
}
